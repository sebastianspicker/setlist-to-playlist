# Apple Music API – Developer Token (JWT)
# From Apple Developer: Keys for your App, then create a key with Music Kit.
# See docs/tech/apple-music.md for setup.
APPLE_TEAM_ID=your-team-id
APPLE_KEY_ID=your-key-id
APPLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----"

# setlist.fm API (used by API proxy; key never sent to client)
# See docs/tech/setlistfm.md for setup.
SETLISTFM_API_KEY=your-setlistfm-api-key

# Web app – Apple Music app identifier (from Apple Developer)
NEXT_PUBLIC_APPLE_MUSIC_APP_ID=your-app-id

# Web app – API base URL for dev-token and setlist proxy (dev: same-origin or API URL)
NEXT_PUBLIC_API_URL=http://localhost:3000

# CORS: required in production. When unset, only http://localhost is allowed (DCI-001).
# ALLOWED_ORIGIN=https://your-app.example.com
