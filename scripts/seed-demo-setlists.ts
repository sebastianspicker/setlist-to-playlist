/**
 * Seed demo setlists for local development.
 * Usage: run with ts-node or tsx; fetches or uses fixture setlist data
 * and optionally writes to a local file or mock API for the app to consume.
 */

// TODO: implement – e.g. fetch a few known setlist IDs from setlist.fm and save as JSON fixtures.
