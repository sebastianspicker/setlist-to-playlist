/**
 * Export diagnostics for support or debugging.
 * Usage: run in app context; collects non-sensitive config (e.g. env var names present,
 * API base URLs) and optionally recent error logs, and outputs a JSON or report.
 */

// TODO: implement – output a minimal diagnostic bundle (no secrets).
