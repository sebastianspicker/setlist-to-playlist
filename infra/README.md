# Infrastructure

Reserved for infrastructure and deployment configs when needed:

- **deploy/** – deployment configs (e.g. Vercel, Fly, Render)
- **nginx/** – nginx configs if required
- **terraform/** – Terraform (optional)
